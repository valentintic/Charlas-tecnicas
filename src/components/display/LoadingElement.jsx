import React, { Component } from 'react'

export default function LoadingElement() {

    useEffect(() => {
        // Code to run on every render or when dependencies change
      }, [dependencies]);

    
  
    return (
      <>
        <div>

        </div>
      </>
    )
}
